<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lease.web.admin.mapper.FacilityInfoMapper">
    <resultMap id="BaseResultMap" type="com.lease.model.entity.FacilityInfo">
        <!--@mbg.generated-->
        <!--@Table facility_info-->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="type" jdbcType="TINYINT" property="type"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="icon" jdbcType="VARCHAR" property="icon"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        id,
        `type`,
        `name`,
        icon,
        create_time,
        update_time,
        is_deleted
    </sql>

    <select id="selectListByApartmentId" resultMap="BaseResultMap">
        SELECT id, type, name, icon
        FROM facility_info
        WHERE is_deleted = 0
          AND id in (SELECT facility_id
                     FROM apartment_facility
                     WHERE apartment_id = #{id}
                       AND is_deleted = 0)
    </select>

    <select id="selectListByRoomId" resultMap="BaseResultMap">
        SELECT id, type, name, icon
        FROM facility_info
        WHERE is_deleted = 0
          AND id in (SELECT facility_id
                     FROM room_facility
                     WHERE room_id = #{id}
                       AND is_deleted = 0)
    </select>
</mapper>