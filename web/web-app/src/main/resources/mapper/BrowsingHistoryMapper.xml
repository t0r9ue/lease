<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lease.web.app.mapper.BrowsingHistoryMapper">
    <resultMap id="BaseResultMap" type="com.lease.model.entity.BrowsingHistory">
        <!--@mbg.generated-->
        <!--@Table browsing_history-->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="room_id" jdbcType="BIGINT" property="roomId"/>
        <result column="browse_time" jdbcType="TIMESTAMP" property="browseTime"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="is_deleted" jdbcType="TINYINT" property="isDeleted"/>
    </resultMap>

    <resultMap id="HistoryItemVoMap" type="com.lease.web.app.vo.history.HistoryItemVo" autoMapping="true">
        <id property="id" column="id"/>
        <result property="roomId" column="room_id"/>
        <collection property="roomGraphVoList" ofType="com.lease.web.app.vo.graph.GraphVo" select="selectGraphListByRoomId" column="room_id"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        id,
        user_id,
        room_id,
        browse_time,
        create_time,
        update_time,
        is_deleted
    </sql>

    <select id="selectPageByUserId" resultType="com.lease.web.app.vo.history.HistoryItemVo">
        SELECT bh.id,
               bh.user_id,
               bh.room_id,
               bh.browse_time,
               ri.room_number,
               ri.rent,
               ai.name apartment_name,
               ai.district_name,
               ai.city_name,
               ai.province_name
        FROM browsing_history bh
                 JOIN room_info ri
                      ON bh.room_id = ri.id
                 JOIN apartment_info ai
                      ON ri.apartment_id = ai.id
        WHERE bh.is_deleted = 0 AND bh.user_id = #{userId}
        ORDER BY browse_time desc
    </select>

    <select id="selectGraphListByRoomId" resultType="com.lease.web.app.vo.graph.GraphVo">
        SELECT name, url
        FROM graph_info
        WHERE is_deleted = 0 AND item_type = 2 AND item_id = #{room_id}
    </select>
</mapper>